source 'https://cdn.cocoapods.org/'

platform :ios, '13.0'

install! 'cocoapods', :generate_multiple_pod_projects => true

inhibit_all_warnings!

target 'BiketoTibet' do
  pod 'Kingfisher/SwiftUI'
end
